/**
 * Grade 3 · Module E3.3 — Writing
 * Gateway GW-E3-W. Skills SK-E3-301 … SK-E3-305 (AGA-CUR-001 §2).
 *
 * NOTE: Writing is OPEN RESPONSE. These skills are not multiple-choice and are
 * NOT sampled by placement. They are graded later by the AI evidence path
 * (PLACEMENT-SPEC §13 — "Nova Star report" masking): a model reads the child's
 * paragraph and reports evidence to the mastery engine; it never sets the band.
 * Multiple-choice sub-skill items (e.g. "which sentence is the best opinion?")
 * may still be authored here as scaffolding practice.
 */
import type { SkillQuestionPool } from "../../types";
import { emptyPool } from "../../_helpers";

export const E3_3_POOLS: SkillQuestionPool[] = [
  emptyPool("SK-E3-301"),
  emptyPool("SK-E3-302"),
  emptyPool("SK-E3-303"),
  emptyPool("SK-E3-304"),
  emptyPool("SK-E3-305"),
];
