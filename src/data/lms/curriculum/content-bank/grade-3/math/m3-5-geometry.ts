/**
 * Grade 3 · Module M3.5 — Geometry
 * Gateway GW-M3-GEO. Skills SK-M3-501 … SK-M3-505 (AGA-CUR-001 §2).
 * Empty shelves — push BankQuestions into each pool's `questions` when authoring.
 */
import type { SkillQuestionPool } from "../../types";
import { emptyPool } from "../../_helpers";

export const M3_5_POOLS: SkillQuestionPool[] = [
  emptyPool("SK-M3-501"),
  emptyPool("SK-M3-502"),
  emptyPool("SK-M3-503"),
  emptyPool("SK-M3-504"),
  emptyPool("SK-M3-505"),
];
