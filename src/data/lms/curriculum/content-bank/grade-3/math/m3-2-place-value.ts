/**
 * Grade 3 · Module M3.2 — Place Value, Rounding, Addition, and Subtraction
 * Gateway GW-M3-NBT. Skills SK-M3-201 … SK-M3-205 (AGA-CUR-001 §2).
 * Empty shelves — push BankQuestions into each pool's `questions` when authoring.
 */
import type { SkillQuestionPool } from "../../types";
import { emptyPool } from "../../_helpers";

export const M3_2_POOLS: SkillQuestionPool[] = [
  emptyPool("SK-M3-201"),
  emptyPool("SK-M3-202"),
  emptyPool("SK-M3-203"),
  emptyPool("SK-M3-204"),
  emptyPool("SK-M3-205"),
];
